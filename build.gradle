buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:1.0.1'
    classpath 'com.novoda:bintray-release:0.2.7'
  }
}

allprojects {
  repositories {
    jcenter()
  }
}